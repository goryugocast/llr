# ブランチ状況メモ (2026-03-31)

公開準備と実験的な流れが混ざらないようにするための、現時点のブランチ整理メモ。

## 現在の基本方針

- `main` は公開・リリース準備の基準線として使う
- 仕様がまだ膨らんでいる流れは、別ブランチに分けて保持する
- すぐにはマージしない流れでも、後から拾えるように意味を明示しておく

## 1. `main`

役割:

- 現在の基準線
- ここから公開準備用の枝を切る

状態:

- 思想ベースに寄せた `README` は取り込み済み
- AI companion 的な docs / onboarding / guide note 実装は未投入
- 以後の release 整備はこのブランチ基点で進める

## 2. `codex/ai-companion-experiment`

役割:

- AI 併用前提の docs / skill 構想 / onboarding 導線の実験枝

含まれているもの:

- AI仕様入口
- AI支援方針
- AIスキル構想
- Getting Started / 初回導線仕様
- plugin 内 onboarding 実装
- ガイドノートの markdown asset 化

現時点の扱い:

- まだ `main` に入れない
- 仕様と実装の目星が立ってから再評価する
- 必要なら README や docs の一部だけを cherry-pick する

## 3. `codex/release-prep`

役割:

- `main` から切った、公開・リリース準備専用ブランチ

想定タスク:

- README の公開向け仕上げ
- LICENSE / manifest / package metadata 確認
- 公開してよい docs の整理
- BRAT 前提の導線確認
- 初回リリース用の最低限整備

状態:

- 現在の `main` と同じ位置に揃えてある
- ここから release 作業を始めてよい

## 4. `codex/srs-exploration`

役割:

- SRS 機能の検討・仕様化・試作を進めるための独立ブランチ

現時点の状態:

- 詳細仕様はまだ未着手
- 現在の `main` と同じ位置に揃えてある
- ここから SRS 検討を始めるための空の検討枝

想定タスク:

- 要件整理
- 既存の routine / repeat / daily note との関係整理
- 仕様メモ
- 必要なら試作

## 5. 補足

- 旧 `codex/release-readme-minimum` は `codex/ai-companion-experiment` に改名した
- 旧 `codex/release-cleanup-next` は整理完了に伴い削除した
- 以後、新しい作業は目的ごとに `main` から枝を切る
